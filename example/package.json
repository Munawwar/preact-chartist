{
  "name": "react-chartist",
  "scripts": {
    "browser-sync": "browser-sync start --server --files './bundle.js'",
    "build": "esbuild main.jsx --outfile=bundle.js --bundle --jsx-factory=h --jsx-fragment=Fragment"
  },
  "devDependencies": {
    "browser-sync": "^2.10.0",
    "esbuild": "^0.15.6",
    "watchify": "^4.0.0"
  },
  "dependencies": {
    "chartist": "^0.10.1",
    "preact": "^8.5.3"
  }
}
